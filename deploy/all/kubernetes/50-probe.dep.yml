---
# Generated by boilr at Sat, 09 Jul 2016 19:55:50 AEST
apiVersion: "extensions/v1beta1"
kind: "Deployment"
metadata:
  labels:
    application: "blackbox-exporter"
    role: "probe"
  name: "probe"
  namespace: "docs-littleman-co"
spec:
  replicas: 1
  selector:
    matchLabels:
      application: "blackbox-exporter"
      role: "probe"
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: "RollingUpdate"
  template:
    metadata:
      labels:
        application: "blackbox-exporter"
        role: "probe"
    spec:
      volumes:
      configMap:
      - name: "probe-config"
        configMap:
          name: "config"
      containers:
      - name: "blackbox-exporter"
        image: "prom/blackbox-exporter:latest" # Todo: Update this
        imagePullPolicy: "Always"
        ports:
        - containerPort: 9115
          protocol: "TCP"
          name: "http"
	volueMounts:
        - name: "probe-config"
          readOnly: false
          mountPath: "/config"
        livenessProbe:
          httpGet:
            # Path to probe; should be cheap, but representative of typical behavior
            path: "/probe"
            port: 9115
          initialDelaySeconds: 5
          timeoutSeconds: 1
        readinessProbe:
          httpGet:
            # Path to probe; akin to a status check. 
            path: "/probe"
            port: 9115
          initialDelaySeconds: 5
          timeoutSeconds: 1
      restartPolicy: "Always"
      securityContext: {} 
