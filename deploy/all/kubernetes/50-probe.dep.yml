---
# Generated by boilr at Sat, 09 Jul 2016 19:55:50 AEST
apiVersion: "extensions/v1beta1"
kind: "Deployment"
metadata:
  labels:
    application: "blackbox"
    role: "probe"
  name: "probe"
  namespace: "docs-littleman-co"
spec:
  replicas: 1
  selector:
    matchLabels:
      application: "blackbox"
      role: "probe"
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: "RollingUpdate"
  template:
    metadata:
      labels:
        application: "blackbox"
        role: "probe"
    spec:
      volumes:
      - name: "probe-config"
        configMap:
          name: "config"
      containers:
      - name: "blackbox"
        image: "prom/blackbox:latest" # Todo: Update this
        imagePullPolicy: "Always"
        args:
        - "-config.file=/config/blackbox.yml"
        ports:
        - containerPort: 9115
          protocol: "TCP"
          name: "http"
        volumeMounts:
        - name: "probe-config"
          readOnly: false
          mountPath: "/config"
        livenessProbe:
          tcpSocket:
            # Path to probe; should be cheap, but representative of typical behavior
            port: 9115
          initialDelaySeconds: 5
          timeoutSeconds: 1
        readinessProbe:
          tcpSocket:
            port: 9115
          initialDelaySeconds: 5
          timeoutSeconds: 1
      restartPolicy: "Always"
      securityContext: {} 
