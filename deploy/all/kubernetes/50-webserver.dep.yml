---
# Generated by boilr at Sat, 09 Jul 2016 15:07:23 AEST
apiVersion: "extensions/v1beta1"
kind: "Deployment"
metadata:
  labels:
    application: "nginx"
    role: "webserver"
  name: "webserver"
  namespace: "docs-littleman-co"
spec:
  replicas: 2
  selector:
    matchLabels:
      application: "nginx"
      role: "webserver"
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: "RollingUpdate"
  template:
    metadata:
      labels:
        application: "nginx"
        role: "webserver"
    spec:
      volumes:
        - name: "etcssl"
          secret:
            secretName: tls-certificates
      containers:
      - name: "nginx"
        image: "gcr.io/littlemanco/webserver:{{GIT_HASH}}" 
        imagePullPolicy: "IfNotPresent"
        ports:
        - containerPort: 80
          protocol: "TCP"
          name: "http"
        - containerPort: 443
          protocol: "TCP"
          name: "https"
        volumeMounts:
        - name: "etcssl"
          readOnly: true
          mountPath: "/etc/ssl"
        livenessProbe:
          httpGet:
            # Path to probe; should be cheap, but representative of typical behavior
            path: "/"
            port: 80
            httpHeaders:
              - name: "Host"
                value: "docs.littleman.co" 
          initialDelaySeconds: 5
          timeoutSeconds: 1
        readinessProbe:
          httpGet:
            # Path to probe; akin to a status check. 
            path: "/"
            port: 80
            httpHeaders:
              - name: "Host"
                value: "docs.littleman.co"
          initialDelaySeconds: 5
          timeoutSeconds: 1
      restartPolicy: "Always"
      securityContext: {} 
