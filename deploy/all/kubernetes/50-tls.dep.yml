---
# Generated by boilr at Sat, 09 Jul 2016 14:57:29 AEST
apiVersion: "extensions/v1beta1"
kind: "Deployment"
metadata:
  labels:
    application: "letsencrypt"
    role: "webserver"
  name: "tls"
  namespace: "docs-littleman-co"
spec:
  replicas: 1
  selector:
    matchLabels:
      application: "letsencrypt"
      role: "webserver"
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: "RollingUpdate"
  template:
    metadata:
      labels:
        application: "letsencrypt"
        role: "webserver"
    spec:
      containers:
      - name: "letsencrypt"
        env:
        - name: "EMAIL"
          value: "hello@andrewhowden.com"
        - name: "DOMAINS"
          value: "docs.littleman.co"
        - name: "DEPLOYMENTS"
          value: "webserver"
        - name: "SECRET_NAME"
          value: "tls-certificates"
        - name: "NAMESPACE"
          value: "docs-littleman-co"
        image: "ployst/letsencrypt:latest"
        imagePullPolicy: "Always"
        ports:
        - containerPort: 80
          protocol: "TCP"
          name: "http"
        livenessProbe:
          tcpSocket:
            port: 80          
          initialDelaySeconds: 5
          timeoutSeconds: 30
        readinessProbe:
          tcpSocket:
            port: 80
          initialDelaySeconds: 5
          timeoutSeconds: 30
      restartPolicy: "Always"
      securityContext: {} 
