---
# Generated by boilr at Sat, 09 Jul 2016 14:57:29 AEST
apiVersion: "extensions/v1beta1"
kind: "Deployment"
metadata:
  labels:
    application: "letsencrypt"
    role: "webserver"
  name: "tls"
  namespace: "letsencrypt-littleman-co"
spec:
  replicas: 1
  selector:
    matchLabels:
      application: "letsencrypt"
      role: "webserver"
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: "RollingUpdate"
  template:
    metadata:
      labels:
        application: "letsencrypt"
        role: "webserver"
    spec:
      # volumes:
      # - name: "tls-etc-conf-d"
      # configMap:
      #   name: "tls-etc-conf-d"
      # - name: "tls-data"
      #   hostPath:
      #     path: /data/tls/
      containers:
      - name: "letsencrypt"
        # env:
        # - name: "FOO"
        #   value: "BAR"
        image: "ployst/letsencrypt:latest" # Todo: Update this
        imagePullPolicy: "Always"
        # args:
        #   - "command"
        #   - "line" 
        #   - "arguments"
        ports:
        - containerPort: 80
          protocol: "TCP"
          name: "http"
        # volumeMounts:
        # - name: "tls-etc-conf-d"
        #   readOnly: true
        #   mountPath: "/etc/tls/conf.d"
        # - name: "tls-data
        #   readOnly: false
        #   mountPath: "/data"
        livenessProbe:
          tcpSocket:
            port: 80          
          initialDelaySeconds: 5
          timeoutSeconds: 30
        readinessProbe:
          tcpSocket:
            port: 80
          initialDelaySeconds: 5
          timeoutSeconds: 30
      restartPolicy: "Always"
      securityContext: {} 
